# 项目背景

目标：单人借助 AI 先做出一款有正式剧情、可玩并准备在抖音上线的互动影像游戏，再逐渐完善可配置框架和多游戏管理后台。代码是通用引擎，游戏实例由 gameId、剧情图、视频素材、变量、主题与版本构成。

协作意图：AI 不只执行单次编码任务，还要随实现、验证和产品决策持续梳理设计理念，使它们落入代码边界、验收条件和仓库文档。任何新会话都应能仅通过根状态文档与相关模块文档，恢复项目意图、最新现状、已采用决策和当前优先级。

已确定：Game Runtime 使用 Cocos Creator 3.8.8 + TypeScript；Game Server 使用 Node.js + TypeScript + PostgreSQL；发布顺序为抖音 → 微信 → Steam。Steam 的桌面封装、SDK 和视频路径需后续验证，不假设可直接复用所有平台能力。

Admin Studio 是 Web 管理后台，负责创建游戏、上传素材、编辑流程/选择/变量条件、预览和发布。前端 UI 框架、节点编辑库、服务框架、ORM、云厂商尚未选择。

视频可来自云端 AI 生成（原讨论为 Seedance 2.0），生成供应商不是框架依赖；第一版采用人工生成后上传，不接生成 API。大文件存对象存储/CDN，代码仓库只保留素材索引。

近期目标：先用技术测试配置验证播放与分支，再制作正式剧情与素材，完成首款游戏的抖音真机和上线验证。首款游戏允许人工维护、校验和发布配置，不以完整 Server/Admin Studio 为前置条件；仍保留共享契约、素材引用、版本号、平台隔离与本地存档边界，减少后续接入后台的返工。首版只要求本机存档，不要求账号或跨设备同步。账号关联、完整后台、商业化、统计、微信和 Steam 均在后续阶段。

待确认：首款游戏的剧情规模、内容制作与权利证明、配置和视频的实际分发方案；这些不能从现有 demo-game 占位配置推定。
