# AI 开发协作规则

## 每次工作前

依次阅读 AGENTS.md、PROGRESS.md、TODO.md、PROJECT_CONTEXT.md、ROADMAP.md、DECISIONS.md；再阅读本次涉及模块的 README 与 docs。不要要求用户重复已有信息。
说明当前阶段、任务范围和假设。有影响实现的歧义先说明并提问，不能把猜测写成既定决定。
多步骤任务采用：`1. [行动] → verify: [验收检查]`，每一步有可验证的成功标准。

## 项目记忆与文档职责

仓库文档是跨会话的唯一项目事实源，不依赖 AI 会话记忆。每次实现、设计讨论或验证后，先判断新信息属于哪类，再更新对应文档：

- PROJECT_CONTEXT.md：长期目标、产品意图、范围与已确定技术方向。
- ROADMAP.md：里程碑、依赖与可验收的完成条件；未验收不改状态。
- PROGRESS.md：截至当前的实际进度、下一步、阻塞和最新验证。
- TODO.md：可执行的近期任务和优先级，不用来代替长期路线。
- DECISIONS.md：会影响后续实现的已采用决策、理由与替代关系。
- CHANGELOG.md：已发生的重要功能、架构和文档机制变化。
- 模块 README 与 docs：当前模块边界、使用方式、协议、约束和验收细节。

文档必须反映已实现和已验证的真实状态。若代码、配置、计划与文档不一致，本次任务内能确认的立即修正；无法确认的显式记为待决定或阻塞，不自行补全。

## 执行边界

- 用户最新明确指令优先。只实现当前任务，禁止把整个路线图一次写完。
- 用最少代码解决问题；不为未来平台提前实现 SDK、支付、插件系统、微服务或通用工作流引擎。
- 只修改必要文件，保持既有风格；不顺便重构或删除无关死代码。
- 新组件、方法和关键规则添加必要的中文注释，重点解释意图。
- 具体游戏内容只能放在配置与 examples，不能写死进核心代码。
- Runtime 核心不能依赖 tt、wx、Steam SDK 或后台；平台 API 仅由 platform 层调用。
- Shared 不依赖任何应用、Cocos、数据库或平台 SDK。禁止使用 eval / Function 执行配置。
- 不将密钥、真实用户资料、大视频、node_modules、编辑器缓存提交进仓库。
- 未实现功能标记 TODO 或显式说明，不伪造成功登录、上传、发布与测试结果。

## 完成后必须做

运行与修改相关的验证；Bug 先建立复现测试，再修复。测试未运行、失败或依赖真机的部分如实记录。
更新 PROGRESS.md（日期、已完成、下一步、阻塞、验证）、TODO.md（勾选或新增近期任务）。达到验收条件才更新 ROADMAP 状态；重要变化写 CHANGELOG。禁止仅因文件存在就标记业务功能完成。
交接应说明做了什么、如何验证、剩余问题。

## 计划变更流程

1. 对照 ROADMAP 和 DECISIONS 分析最新用户指令，说明影响和最小调整方案。
2. 已获授权的调整直接执行；缺少关键产品决定时先询问，不擅自替换技术路线。
3. 同步 ROADMAP、TODO、PROGRESS；涉及长期背景时更新 PROJECT_CONTEXT。
4. 新增有日期、状态、原因、影响的 DECISIONS 记录；旧决策保留并标注“已替代”及新编号。
5. 更新 CHANGELOG，检查后续任务依赖和文档一致性。

禁止过度工程化：不增加用户未要求的功能、一次性抽象、虚假兼容层和无依据的配置项。若 50 行能完成，不写 200 行。
