# Admin Studio

Web 管理后台骨架，按新路线图在 M6 完善，不是首款游戏上线前置。UI 框架尚未选定；没有可启动的后台页面。

首版顺序：游戏列表/基本配置 → 视频素材库 → 剧情图 → 节点与变量条件编辑 → 校验/预览 → 发布历史。详见 docs/admin-studio.md。

src/features 为功能边界，src/api 为后端访问层，src/models 保存仅后台使用的编辑状态。画布坐标不进入 Runtime 配置。所有写入服务端的数据必须校验；管理员权限由服务端强制执行。
